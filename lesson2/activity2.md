### @explicitHints true
### @hideIteration true 
# Aufgabe 2 – Bring die Lampen zum Leuchten

```python
blocks.place(GRASS_BLOCK, pos(0, 0, 0))
```

## Schritt 1
Setze **Redstone-Lampen** (redstone lamp) auf den angegebenen Höhen in die Lampenhalterungen. Der zweite Parameter von `blocks.place()` gibt die Position an. Ändere dort in `pos(x, y, z)` die **zweite Zahl**, also die Höhe `y`.
