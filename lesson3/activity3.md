### @explicitHints true

# Aufgabe 3 – Stimmt die Rechnung?

```python
player.say("Hallo")
preis += 1
```

## Schritt 1
Schau dir die Rechnung auf Bildschirm 1 an. Erstelle eine Variable namens `gesamtkosten`, in der du das Ergebnis speicherst. Führe den Code aus, damit die Gesamtkosten im Chat erscheinen. Drücke dann den Knopf mit dem passenden Ergebnis. Die Variablen `apfel`, `melone`, `beeren` und `kartoffel` enthalten die Preise der jeweiligen Lebensmittel.

## Schritt 2
Auf Bildschirm 2 kommt ein Kürbis dazu. Erstelle dafür die Variable `kürbis`. Berechne ihren Wert als Differenz aus `beeren` und `apfel`, also Beerenpreis minus Apfelpreis. Rechne `kürbis` zu den Gesamtkosten in `gesamtkosten` dazu. Führe den Code aus und drücke den passenden Knopf.

## Schritt 3
Auf dem letzten Bildschirm haben sich zwei Preise geändert: Der Apfel (`apfel`) kostet jetzt **2 mehr**, die Melone (`melone`) **3 weniger**. Passe die Werte mit `+=` und `-=` an. Damit kannst du einen Wert erhöhen oder verringern.

```template
apfel = 10
melone = 15
beeren = 20
kartoffel = 2
// Ersetze die folgenden Hinweise durch deinen Code.
// Lege die Variable kürbis an. | Schritt 2
// Passe den Wert von apfel an. | Schritt 3
// Passe den Wert von melone an. | Schritt 3
// Berechne die Gesamtkosten und speichere sie in gesamtkosten. | Schritt 1
player.say(gesamtkosten)
```
