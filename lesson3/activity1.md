### @explicitHints true

```python
.
```

# Aufgabe 1 – Alles in der richtigen Reihenfolge

## Schritt 1
Erstelle **zwei Variablen** mit den Namen `b` und `c`. Eine Variable speichert einen Wert unter einem Namen. Speichere in `b` den Text **"melon"** (Melone) und in `c` den Text **"apple"** (Apfel). Die vorgegebene Variable `a` enthält **"berries"** (Beeren). Verwende diese englischen Wörter, damit sie zur Aufgabe im Spiel passen.
### ~ tutorialhint
Textwerte heißen auch **Strings**. Schreibe sie in Anführungszeichen.
 
## Schritt 2
Schau dir die Reihenfolge auf dem Bildschirm an der Wand an. Passe die Zuweisungen an die Variable `fruit` (Obst) so an, dass die Ausgabe dazu passt. Führe den Code aus und drücke die Obstknöpfe in derselben Reihenfolge wie im Chat. Mache weiter, bis alle Lampen leuchten.

```template
// Ersetze die folgenden Hinweise durch deinen Code.
a = "berries"
// Lege die Variable b an. | Schritt 1
// Lege die Variable c an. | Schritt 1
d = a
// Weise fruit den passenden Wert zu. | Schritt 2
player.say(fruit)

// Weise fruit den passenden Wert zu. | Schritt 2
player.say(fruit)

// Weise fruit den passenden Wert zu. | Schritt 2
player.say(fruit)

fruit = d
player.say(fruit)

``` 